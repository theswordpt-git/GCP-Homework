## README

This repository contains the required Terraform deliverables for the assignment. All items below were completed and a screenshot of a successful deployment is included.

- Screenshot: Successful Terraform apply showing output and created files (see included image file).
- Provider configuration: Terraform file using the latest Google provider.
- GCP VPC configuration: Terraform file creating a VPC using example code from the Terraform Registry.
- Local file: A text file generated by Terraform (local_file resource) containing my favorite food.
- Output: Terraform output block configured to display the VPC name in GCP.

Files included:
- screen-shot.png (successful deployment)
- 0-authentication.tf (has provider block in it)
- example-vpc-brazenly-stolen.tf (obviously stolen from terraform documentation, includes URL in comments)
- favorite-food.tf (spits out favorite.txt via local_file)
- outputs.tf (spits out the name of the example vpc)

